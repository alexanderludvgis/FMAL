#ifndef PARSER_H
#define PARSER_H

#include <iostream>
#include <string>

using namespace std;	// has to be included so "string" variable works.


class Parser
{
    public:
        Parser();
        virtual ~Parser();
    protected:
    private:
};

#endif // PARSER_H
